================================================
AI BRIEFING SYSTEM — Quick Reference Card
Production v3.2 — Feb 2026
================================================

INFRASTRUCTURE
  Droplet:   DigitalOcean (Ubuntu)
  n8n UI:    http://<YOUR_DROPLET_IP>:5678

WORKFLOWS (4 total):
  Equity Daily:      Mon-Fri 6:30 AM AST (10:30 UTC)
  Equity Saturday:   Sat 7:00 AM AST (11:00 UTC)
  Treasury Daily:    Mon-Fri 6:30 AM AST (10:30 UTC)
  Treasury Saturday: Sat 7:00 AM AST (11:00 UTC)

NODE MAP (same for all workflows):
  Schedule → Fetch Data → Code (Claude) → Process Response → Google Drive → Gmail

EQUITY CODE FILES:
  NODE1_fetch_market_data.js   → Fetch Market Data (both daily + saturday)
  NODE2_claude_daily.js        → Code node (daily only)
  NODE2_claude_saturday.js     → Code node (saturday only)
  NODE3_process_response.js    → Process Response (both)

TREASURY CODE FILES:
  NODE1_fetch_data.js          → Fetch Treasury Data (both)
  NODE2_claude_daily.js        → Code node (daily only)
  NODE2_claude_saturday.js     → Code node (saturday only)
  NODE3_process_response.js    → Process Response (both, with chart embed)

N8N EXPRESSIONS:
  Gmail Subject:    {{ $('Process Response').first().json.emailSubject }}
  Gmail Message:    {{ $('Process Response').first().json.emailHtml }}
  Google Drive key: data

MONTHLY COST:
  DigitalOcean:  $6
  Claude API:    $8-15 (equity) + $8-15 (treasury)
  Alpha Vantage: $0 (free tier)
  Finnhub:       $0 (free tier)
  Google/Gmail:  $0
  Total:         ~$22-36/mo

POST-MORTEM RULES (memorize these):
  1. ALWAYS use Code nodes for Claude API calls. Never HTTP Request.
  2. NEVER use template literals (backticks) in n8n code.
  3. Right-click .js files → Open With → Notepad. Never double-click.
  4. After pasting API key, verify closing single-quote + semicolon.
  5. Wait 60s between manual Claude node tests.
  6. Binary field key = "data" (must match Google Drive config).
  7. Use explicit node refs: $('Node Name').first().json.field
  8. Use closest-business-day logic for all date-based API lookups.
  9. Delete orphan nodes before adding replacements.

TROUBLESHOOTING:
  Claude timeout     → Increase Code node timeout (180s daily, 240s saturday)
  AV yields N/A      → Free tier limit hit or holiday. Brief still generates.
  Web search fails   → Brief notes "data unavailable". Normal.
  Copy-paste breaks  → Retype the line. Check for curly quotes.
  Windows error      → You double-clicked the .js file. Right-click → Open With → Notepad.
